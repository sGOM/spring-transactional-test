# CLAUDE.md

이 저장소에서 작업할 때 Claude Code 가 따라야 할 규칙이다.

## 이 프로젝트는 무엇인가

Spring + JPA 의 트랜잭션 동작을 **실행 가능한 테스트로 증명**하는 학습/레퍼런스 프로젝트다.
전파 속성, 롤백 규칙, 프록시 AOP 의 한계, 트랜잭션 이벤트, 영속성 컨텍스트, 격리 수준,
동시성 락을 각각 Kotest `BehaviorSpec` 으로 고정해 두었다.

- Kotlin 2.2.21 / JDK 17 / Spring Boot 4.0.1 / Kotest 5.9.1 / H2(인메모리, MVStore)
- 테스트 목록과 각 시나리오의 기대 결과는 `README.md` 에 표로 정리되어 있다.
  **테스트 관련 질문에 답하기 전에 `README.md` 의 해당 섹션을 먼저 확인한다.**

---

## 응답 규칙 (가장 중요)

### 1. 테스트에 대한 질문에는 "쉬운 설명 → 자세한 설명" 순서로 답한다

사용자가 특정 테스트, 스펙, 시나리오, 트랜잭션 동작에 대해 물으면 **반드시 아래 2단 구조**로 답한다.
곧바로 코드나 용어부터 꺼내지 않는다.

**1단계 — 한눈에 (쉬운 설명)**

- 3~5줄. 전문 용어를 최소화하고, 써야 한다면 그 자리에서 한 줄로 풀어 준다.
- "무엇을 확인하는 테스트인가 / 결과가 어떻게 나오는가 / 왜 그렇게 되는가"를 이 순서로 요약한다.
- 비유나 아주 짧은 도식은 여기서 쓴다.

**2단계 — 자세히**

- 실행 흐름을 단계별로 추적한다. 어느 시점에 트랜잭션이 열리고 / 참여하고 / 중단(suspend)되고 /
  커밋·롤백되는지 명시한다.
- 근거가 되는 코드를 `파일경로:줄번호` 형태로 인용한다 (예: `RequiredPropagationTest.kt:47`).
  서비스 계층 동작이 관련되면 테스트뿐 아니라 `service/` 쪽 코드도 함께 짚는다.
- 물리 트랜잭션 / 논리 트랜잭션, rollback-only 오염, 1차 캐시 개입 등 **핵심 개념을 명시적으로 이름 붙여** 설명한다.
- 헷갈리기 쉬운 인접 케이스와 대비시킨다 (예: REQUIRED vs REQUIRES_NEW, NESTED 가 JPA 에서 왜 안 되는지).
- 실무에서 이 동작이 어떤 사고로 드러나는지 한 줄 덧붙인다.

두 단계는 `### 한눈에` / `### 자세히` 같은 헤딩으로 눈에 띄게 구분한다.
사용자가 "짧게", "요약만" 이라고 하면 1단계만 답하고 멈춘다.

### 2. 추측하지 말고 확인한다

트랜잭션 동작은 직관과 어긋나는 지점이 많다. 기억에 의존해 단정하지 말고
해당 테스트 파일과 서비스 코드를 실제로 읽은 뒤 답한다.
확실하지 않으면 "테스트를 돌려서 확인하겠다"고 말하고 실행한다.

### 3. 답변 언어

모든 설명은 한국어로 한다. 코드 식별자, 애노테이션, 예외 클래스명, 전파 속성 이름
(`REQUIRES_NEW`, `UnexpectedRollbackException` 등)은 원문 그대로 쓴다.

---

## 명령어

```bash
./gradlew test                                  # 전체 테스트
./gradlew test --tests '*RequiredPropagationTest'   # 특정 스펙만
./gradlew build
```

트랜잭션 경계를 눈으로 확인하려면 `src/main/resources/application.yaml` 의
`org.springframework.transaction.interceptor: TRACE` 두 줄 주석을 해제한다.

---

## 코드를 수정할 때 지켜야 할 설계 결정

이 프로젝트의 테스트는 "통과시키는 것"이 목적이 아니라 "동작을 증명하는 것"이 목적이다.
아래 규칙을 어기면 테스트가 초록불이면서도 아무것도 증명하지 못하는 상태가 된다.

1. **테스트 메서드에 `@Transactional` 을 붙이지 않는다.**
   테스트를 트랜잭션으로 감싸면 서비스의 REQUIRED 가 테스트 트랜잭션에 참여해 버려
   검증 대상인 커밋/롤백 경계가 사라진다. 정리는 `DatabaseCleaner.clean()` 의 TRUNCATE 로 한다.

2. **모든 실행과 검증은 `Then` 블록 안에 둔다.**
   Kotest 의 `beforeTest` 는 컨테이너(`Given`/`When`)에도 걸리므로,
   `When` 본문에서 동작을 실행하면 뒤이은 초기화에 데이터가 지워진다.

3. **물리 트랜잭션은 EntityManager 의 identity 로 식별한다** (`support/TxProbe.kt`, `TxSnapshot.kt`).
   REQUIRED 는 같고 REQUIRES_NEW 는 다르다.

4. **동시성 테스트는 `Thread.sleep` 대신 `support/Concurrency.kt` 의 `Signal` 로 순서를 고정한다.**
   `CountDownLatch` 로 진행 순서를 못 박고, 대기 시간이 지나면 조용히 통과시키지 말고 실패시킨다.

5. **격리 수준을 다루는 테스트에서는 재조회 전에 `clear()` 를 호출한다.**
   1차 캐시가 격리 수준을 가려 실험 자체가 무의미해진다.

6. **각 스펙 상단의 KDoc 블록을 유지한다.** 검증 항목을 번호로 나열하는 형식이며,
   테스트를 추가·변경하면 이 KDoc 과 `README.md` 의 표도 함께 갱신한다.

7. 순서가 보장되지 않는 동작(같은 phase 의 이벤트 리스너 등)은 단일 정답으로 단언하지 말고
   허용 가능한 복수 결과로 단언한다.
